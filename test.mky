let numberToText = fn(x) {
    if (x == 0) {
        return "zero"
    }

    if (x == 1) {
        return "one"
    }

    if (x == 2) {
        return "two"
    }

    if (x == 3) {
        return "thee"
    }

    if (x == 4) {
        return "four"
    }

    if (x == 5) {
        return "five"
    }

    return "I dunno"
}

let a = 1
let max = 5
while ((a < max) || (5 == a)) {
    printf("The number is %s (%d)\n", numberToText(a), a)
    let a = a + 1
}
println("All done!")